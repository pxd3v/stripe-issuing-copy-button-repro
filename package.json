{
  "name": "stripe-issuing-copy-button-repro",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal reproduction: Stripe Issuing issuingCardCopyButton iframe lacks clipboard-write, so copy silently fails in Chrome",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^16.12.0"
  }
}
